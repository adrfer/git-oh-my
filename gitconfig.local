# For more info about git-config, open a terminal and run:
#
#    git help config
#
# Also, check git-scm.com/docs/git-config
# 
# /!\ Make your own customizations in here

[format]

  # Custom output format to git log and git show
  pretty = format:%C(auto)%h %C(reset)%s %C(blue)(%cr) %C(red)<%an>
